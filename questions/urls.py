from django.urls import path
from questions import views

app_name = 'questions'

urlpatterns = [
    # path('', views.questions_list, name='list'),
    # path('singleChoices/', views.SingleChoicesList.as_view(), name='single_choices_list'),
    # path('singleChoices/<int:pk>', views.SingleChoiceDetail.as_view(), name='single_choice_detail')
]
